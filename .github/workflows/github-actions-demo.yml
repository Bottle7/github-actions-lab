name: GitHub Actions Demo

on:
  push:
    branches:
      - main
    paths-ignore:
      - '.gitignore/**'
      
  pull_request:
    branches:
      - main
  
  schedule:
    - cron: '15 6 * * SUN'
    
  workflow_dispatch:
